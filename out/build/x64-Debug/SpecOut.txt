Edgar genius!

In package kf5-plasma-mediacenter:

In package NetworkManager-applet-gtk:
Obsoletes: NetworkManager-gnome < 0.9.8.4
Provides: NetworkManager-gnome = %version-%release

In package alsa-ucm-conf:

In package apache2:
Obsoletes: %name-common < %EVR
Provides: httpd = %EVR

Provides: httpd2 = %EVR

Provides: %name-common = %EVR

Provides: %apache_configs_name = %apache_configs_version

Provides: %apache_configs_dirs_name = %apache_configs_dirs_version

Provides: %apache_config_tool_name = %apache_config_tool_version

Provides: %name-mmn = %mmn

Provides: %name-httpd = %EVR

Provides: %name-mmn = %mmn

Provides: %name-httpd = %EVR

Provides: %name-httpd-prefork-like = %EVR

Provides: %name-mmn = %mmn

Provides: %name-httpd = %EVR

Provides: %name-mod_authnz_ldap = %EVR

Provides: %%apache_configs_name = %%apache_configs_version

Provides: %%apache_configs_dirs_name = %%apache_configs_dirs_version

provides: %%name-libdb = %%n_dbver

In package apache2-mod_php8.0:
Provides: apache2-mod_php = %php_version

Provides: php-engine = %php_version-%php_release

In package apache2-mod_php8.1:
Provides: apache2-mod_php = %php_version

Provides: php-engine = %php_version-%php_release

In package apache2-mod_php8.2:
Provides: apache2-mod_php = %php_version

Provides: php-engine = %php_version-%php_release

In package avidemux-qt:
Obsoletes: avidemux2 < %version-%release
Provides: avidemux3 = %version-%release

Provides: avidemux2 = %version-%release

Provides: avidemux = %version-%release

Provides: %name-qui = %version-%release

Provides: %name-qui = %version-%release

Provides: %name-qui = %version-%release

In package bird2:
Obsoletes: bird2 < %EVR
Provides: bird2 = %EVR

In package bootloader-utils:

In package branding-simply-linux:
Provides: design-graphics = 12.0.0

Provides: %(for n in %provide_list; do echo -n "$n-release = %version-%release "; done) altlinux-release-%theme  branding-alt-%theme-release

Provides: alt-license-theme = %version alt-notes-%theme

Provides: indexhtml indexhtml-%theme = %version indexhtml-Desktop = 1:5.0

In package branding-xalt-kworkstation:
Provides: design-graphics = %design_graphics_abi_major.%design_graphics_abi_minor.%design_graphics_abi_bugfix

Provides: gnome-session-splash = %version-%release

Provides: %(for n in %provide_list; do echo -n "$n-release = %version-%release "; done) altlinux-release-%theme  branding-alt-%theme-release

Provides: alt-license-theme = %version alt-notes-%theme

Provides: gnome-theme-%brand-%theme = %version-%release

Provides: metacity-theme-%brand-%theme = %version-%release

Provides: gnome-menus = 2.30.4

Provides: indexhtml indexhtml-%theme = %version indexhtml-Desktop = 1:5.0

In package btrfs-progs:

In package cups:
Provides: %name-ppd = %version %name-common = %version

Provides: cups-libs = %version

Provides: %name-devel = %version %name-ddk = %version

In package cups-filters:

In package dpkg:

In package drbd-utils:
Obsoletes: %name-bash-completion < %EVR
Provides: %name-bash-completion = %EVR

In package dtc:

In package duf:

In package extra-cmake-modules:

In package fcoe-utils:

In package ffmpeg-plugin-browser:

In package firefox-esr:
Obsoletes: firefox-esr-$lang < %%EVR"; done)}
Provides: firefox-esr-$lang = %%EVR\nObsoletes: firefox-esr-$lang < %%EVR"; done)}

In package firmware-linux:
Obsoletes: firmware-amd-ucode <= 2.0

In package flacon:
Provides: %name-qt5 = %version

In package gbinder-python:

In package gcompris-qt:

In package kf5-rpm-build:

In package libtsm:

In package libtsmux:

In package libtunepimp:

In package libtweeny:

In package libtxc_dxtn:

In package libu2f-server:

In package libucdn:

In package libucil:
Obsoletes:      libucil < 0.9.10

In package libucl5:

In package libudev0:
Obsoletes: libudev < %version-%release
Provides: libudev = %version-%release

In package libudfread:

In package libudt:
Provides: udt = %{version}-%{release}

Provides: udt-devel = %{version}-%{release}

In package libuhttpmock:

In package libuldaq:

In package libumockdev:

In package libump-mcom02:

In package libuna:

In package libunibreak:

In package libunicap:

In package libunicapgtk:

In package libuninameslist:

In package libuninum:

In package libunique:

In package libunique3:

In package libuniset2:

In package fetchmail:

In package ff-clone:

In package ffmpegthumbnailer:
Obsoletes: lib%{name}3 < %version

In package ffms2:

In package ffsb:

In package fft:

In package fft2d:

In package fftw3:

In package fgo:

In package fgr:

In package fheroes2:

In package fhs:

In package fi_lib:

In package fiaif:

In package fido2:

In package fidoconf:
Provides: %name-devel-libs = %version-%release

Provides: %name-devel-libs = %version-%release

In package figlet:

In package file-roller:

In package file5:

In package fileschanged:

In package filesystem:

In package filezilla:

In package filigram:

In package fillets-ng:

In package fillets-ng-data:

In package fillup:

In package filterXQueryExtension:

In package findbugs:

In package findbugs-bcel:

In package findsym:

In package findutils:

In package finger:

In package fio:

In package firebird:
Obsoletes: %name-client-embedded <= 2.0
Provides: %name-superserver = %EVR

Provides: %name-classic = %EVR

Provides: %name-utils-superserver = %EVR

Provides: %name-utils-classic = %EVR

Provides: %name-server-superserver = %EVR

Provides: %name-server-classic = %EVR

Provides: %name-server-common = %EVR

In package firecracker:

In package firectl:

In package firefox-adblock_plus:

In package firefox-certificate_watch:

In package firefox-dark_reader:

In package firefox-duckduckgo_plus:

In package DirectXShaderCompiler:
Provides: lib%{rname} = %EVR

In package DistroDbMaker:
Obsoletes: python-module-DistroDbMaker < 0.027

In package FlightGear:

In package LibreOffice:
Obsoletes: libreoffice < 3.99

Obsoletes: %name-full < %EVR

Obsoletes: %{name}-langpack-%{-o*} < %{-v*}} \
Provides: %name-full = %EVR

Provides: libreoffice = %EVR

Provides: %uname = %EVR

Provides: %name-gnome = %EVR

Provides: %name-kde = %EVR

Provides: %name-kf5 = %EVR

In package ModemManager:

In package NetworkManager:

In package NetworkManager-openvpn:
Obsoletes: %name-gnome < 0.9.8.4

Obsoletes: %name-gtk < 1.8.18-alt1
Provides: %name-gnome = %version-%release

Provides: %name-gtk = %version-%release

In package NetworkManager-pptp:
Obsoletes: %name-gnome < 0.9.8.4

Obsoletes: %name-gtk < 1.2.10-alt1
Provides: %name-gnome = %version-%release

Provides: %name-gtk = %version-%release

In package NetworkManager-vpnc:
Obsoletes: %name-gnome < 0.9.8.6

Obsoletes: %name-gtk < 1.2.8-alt1
Provides: %name-gnome = %version-%release

Provides: %name-gtk = %version-%release

In package OpenSceneGraph:
Provides: OpenThreads = %name-%version

In package SimGear:
Obsoletes: SimGear-devel < 1.0.0
Provides: SimGear = %version

Provides: SimGear-devel = %version

In package aardvark-dns:

In package accel-ppp:

In package admc:

In package akvis-repo:

In package alacritty:

In package alt-csp-cryptopro:
Obsoletes: alt-csp-cryptopro-mate < %EVR

Obsoletes: alt-csp-cryptopro-kde < %EVR
Provides: alt-csp-cryptopro-mate = %EVR

Provides: alt-csp-cryptopro-kde = %EVR

In package alt-customize-branding:

In package alt-kworkstation-addon:
